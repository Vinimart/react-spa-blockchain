import { initializeContract, initializeProvider } from './blockchainService'
import { addCitizen, getAllCitizens } from './citizensService'

export { initializeProvider, initializeContract, getAllCitizens, addCitizen }
